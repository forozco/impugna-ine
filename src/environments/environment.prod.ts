export const environment = {
  production: true,
  apiUrl: 'https://api.impugna-ine.com',
  environment: 'production',
  useMockData: false, // Nunca usar mock en producción
  mockDataPath: '' // No necesario en producción
};